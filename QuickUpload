import React, { useState } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Upload, Zap } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { createPageUrl } from "@/utils";

export default function QuickUpload({ onAnalysisComplete }) {
  const navigate = useNavigate();

  return (
    <Card className="border-none shadow-lg bg-gradient-to-br from-indigo-50 via-purple-50 to-pink-50">
      <CardHeader>
        <div className="flex items-center gap-3">
          <div className="p-2 bg-white rounded-lg shadow-md">
            <Zap className="w-6 h-6 text-indigo-600" />
          </div>
          <CardTitle className="text-2xl font-bold text-slate-900">
            Quick Start
          </CardTitle>
        </div>
      </CardHeader>
      <CardContent className="space-y-4">
        <p className="text-slate-700 text-lg">
          Upload a resume and get instant AI-powered analysis with actionable suggestions to improve your chances of landing the job.
        </p>
        <Button
          onClick={() => navigate(createPageUrl("Analyzer"))}
          className="bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 shadow-lg hover:shadow-xl transition-all duration-300"
          size="lg"
        >
          <Upload className="w-5 h-5 mr-2" />
          Analyze New Resume
        </Button>
      </CardContent>
    </Card>
  );
}
